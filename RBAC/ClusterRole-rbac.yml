apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: role-developer
rules:
  - apiGroups:
      ["", "apps"]
    resources:
      - pods
      - pods/log
      - deployments
      - services
    verbs:
      - get
      - list
      - watch